import os\nimport json\nimport feedparser\nimport requests\nfrom datetime import datetime\n\n# Fetch news from multiple Korean AI RSS feeds\nFEEDS = [\n    'https://aitimes.kr/feed/',\n    'https://www.venturelab.co.kr/feed/',\n    'https://www.etnews.com/sections/3020300/feed',\n    'https://www.donga.com/news/feed/section/0301',\n    'https://www.hankyung.com/feed/',\n    'https://www.zdnet.co.kr/feed/',\n    'https://www.hankookilbo.com/feed/'\n]\n\ndef fetch_feeds():\n    news_items = []\n    for feed in FEEDS:\n        parsed_feed = feedparser.parse(feed)\n        for entry in parsed_feed.entries:\n            news_items.append({\n                'title': entry.title,\n                'link': entry.link,\n                'source': feed\n            })\n    return news_items\n\ndef analyze_news(news_item):\n    api_key = os.getenv('ANTHROPIC_API_KEY')\n    headers = { 'Authorization': f'Bearer {api_key}' }\n    analysis = requests.post('https://api.anthropic.com/v1/claude/analyze', headers=headers, json={'text': news_item['title']}).json()\n    return {\n        'summary': analysis.get('summary'),\n        'key_points': analysis.get('key_points'),\n        'relevance_score': analysis.get('relevance_score'),\n        'insights': analysis.get('insights'),\n        'recommended_action': analysis.get('recommended_action')\n    }\n\ndef save_results(news_items):\n    timestamp = datetime.now().strftime('%Y%m%d')\n    file_path = f'data/news_{timestamp}.json'\n    with open(file_path, 'w') as f:\n        json.dump(news_items, f, ensure_ascii=False, indent=4)\n\ndef main():\n    news_items = fetch_feeds()\n    analysis_results = []\n    for news_item in news_items:\n        analysis = analyze_news(news_item)\n        analysis_results.append({\n            'timestamp': datetime.now().isoformat(),\n            **news_item,\n            'analysis': analysis\n        })\n    save_results(analysis_results)\n\nif __name__ == '__main__':\n    main()